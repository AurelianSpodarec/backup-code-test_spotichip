import { useParams } from "react-router-dom";

function ShowArtist() {

    const { id }:any = useParams();
    // fetch artist
    // display the name

    return (
        <div>
            
        </div>
    )
}

export default ShowArtist